services:
  - type: worker
    name: chaos-arena-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python chaos_arena_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
